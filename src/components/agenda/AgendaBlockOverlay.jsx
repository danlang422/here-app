// Stub for v1 — renders nothing but exists in the component tree.
// Will render block band overlays once schedule template data is available (Calendar Management).
// eslint-disable-next-line no-unused-vars
function AgendaBlockOverlay({ blockCount, gridStartMinutes }) {
  return null
}

export default AgendaBlockOverlay
